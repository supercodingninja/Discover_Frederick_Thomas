/**
 * @file Implements Stickybits to polyfill sticky position property on sticky blocks.
 */
(function ($, Drupal) {

  "use strict";

  Drupal.behaviors.stickyblock = {
    attach: function (context, settings) {
      stickybits('.sticky', {noStyles: true});
    }
  };
})(jQuery, Drupal);
